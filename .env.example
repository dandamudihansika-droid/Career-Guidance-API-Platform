# Flask Settings
SECRET_KEY=career-guidance-secret

# SMTP Email configurations for Real Email Notifications and OTP
# Configure these variables to receive actual HTML emails instead of console fallbacks.
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password
